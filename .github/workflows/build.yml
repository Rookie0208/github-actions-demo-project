name: build from branch

workflow_dispatch:
    inputs:
      branch_name:
        description: "Branch to test"
        required: true
        default: "main"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: 🛒 Checkout Repository
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.inputs.branch_name }}  # Dynamic branch name

      - name: List repository contents
        run: |
          echo "Listing repository contents..."
          ls -R

      - name: 📦 Set Up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📥 Install Dependencies
        run: npm install

      - name: 🔍 Run Linting
        run: npm run lint

      - name: 🧪 Run Tests
        run: npm test

      - name: 🛠️ Build Project
        run: npm run build

      - name: Build image
        run: docker build -t ./src/ rookie0208/docker-frontent-demo:latest

      - name: push image to docker
        run: docker push rookie0208/docker-frontend-demo:latest
